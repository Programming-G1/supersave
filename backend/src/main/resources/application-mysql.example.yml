spring:
  datasource:
    url: jdbc:mysql://localhost:3306/supersave?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: change-me
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

# TODO:
# 1. Remove the mock profile exclusion from application-mock.yml
# 2. Add JpaRepository implementations and real persistence wiring
# 3. Replace mock repositories with public-data ingestion pipeline
